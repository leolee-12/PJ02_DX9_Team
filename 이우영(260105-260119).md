# 일자별 작업 기록 (260105-260119)

- 담당자 : 이우영
- 역할 : 플레이어, 이펙트, 몬스터, 아이템
- 최신 업데이트 : 26.02.05

### 26.01.05 작업 내용

- CMonsterN2 기어가는 움직임 표현 및 움직임에 따라 마디가 뒤따라오는 모션 구현
-> 이동 시 현재 방향(vCurDir)과 원하는 방향(vDesiredDir)이 이루는 각도를 계산
-> 최대 각도 이하일 경우 vDesireDir 그대로 사용 / 최대 각도 초과 시 최대 회전 각도만큼만 회전한 방향을 사용

```cpp
_vec3 CMonsterN2::Compute_LimitedDir(const _float& fMaxAngle, const _vec3& vCurDir, const _vec3& vDesiredDir)
{
	_vec3 v1, v2;
	D3DXVec3Normalize(&v1, &vCurDir);
	D3DXVec3Normalize(&v2, &vDesiredDir);


	_float fDot = max(-1.f, min(1.f, D3DXVec3Dot(&v1, &v2)));
	_float fRad = acosf(fDot);
	_float fMaxRad = D3DXToRadian(fMaxAngle);

	if (fRad <= fMaxRad)
		return v2;	// 최대 회전 각도보다 작으면 그대로 사용

	// 최대 회전 각도보다 크면 최대 회전 각도로 보정
	fRad = fMaxRad / fRad;

	_vec3 vResult;
	D3DXVec3Lerp(&vResult, &v1, &v2, fRad);
	D3DXVec3Normalize(&vResult, &vResult);
	return vResult;
}
```
ex. vCurDir ~ vDesiredDir 사이 각도 fAngle = 120.f | 최대 회전 각도 fMaxAngle 60.f
-> fRatio = fMaxAngle / fAngle = 60.f / 120.f = 0.5f
-> Lerp(&vResult, &vCurDir, &vDesiredDir, t)
// t = 0.f		: vResult = vCurDir;
// t = fRatio	: vResult = (fMaxAngle만큼 회전한 방향);
// t = 1.f		: vResult = vDesiredDir;


- 급격한 방향 전환 시에도 위와 동일한 방식으로 완전한 전환을 늦추고, 단계적으로 방향을 찾아가도록 하였음
-> 머리 부분의 스프라이트 전환과 마디의 z값으로 인해 부자연스러운 움직임 발생을 방지

```cpp
void CN2_AI::Enter_State(const _uint& iState)
{
	switch (iState)
	{
	case CMonsterN2::N2S_CRAWL:
	{
		m_fAcmlTime = 0.f;

		_vec3 vPrevPos, vDesiredDir;
		if (!m_bChase)
		{
			vDesiredDir = Randomize_Dir();
			m_fSpeed = 0.05f;
		}
		else
		{
			vDesiredDir = Compute_TargetDir();
			m_fSpeed = 0.1f;
		}

		m_pOwnerTC->Get_Info(INFO_POS, &vPrevPos);
		m_vDir = Compute_LimitedDir(90.f, m_vDir, vDesiredDir);
		m_vLerpPos = vPrevPos + m_vDir * 3.f;	// Update에서 Lerp이동 후 STOP State -> 일정 시간 후 CRAWL State -> ... (반복)
	}
		break;
		//...
	}
}
```

### 26.01.06 작업 내용

- CMonsterN2 움직임 디테일 수정
-> 머리의 속도에 따라 마디 간격을 유동적으로 조정(Lerp)
-> 알파소팅용 값인 m_fDepth 임의조정하여 점프 및 급격한 방향 전환 시 머리와 마디의 배치가 부자연스러운 현상 제거
-> 공격(점프) 쿨타임 적용, 점프 시 애니메이션 1회 반복 후 착지 전까지 고정, 점프 높이 및 거리 조정
-> 정해진 이동 방향의 x, z 성분에 난수를 더하여 모든 객체가 이동 동선이 유사하지 않게 조정

### 26.01.07 작업 내용

- CMonsterN2 움직임 디테일 수정
-> 마디 스케일 조정에 의해 공중에 떠있는 느낌을 높이 보정하여 제거 (JUMP 상태 이외일 때)
-> LAND 상태에서 완전히 정지하지 않고, 감속하는 시간을 둬서 마디가 땅에 도달하지 못했을 때 정지하는 현상 수정

- 기본 몬스터 1종 (CMonsterN3 - 박쥐형) 구현
-> 상태 별(FLY, PREPARE, RUSH) 애니메이션 적용, 동작 로직 구현

- 전용 AI 컴포넌트들이 모두 사용하는 거리 및 방향 계산 함수를 추상 클래스(CAIController)로 이관

### 26.01.08 작업 내용

- 중간 점검을 위해 모든 팀원 진행 내용 병합 수행

- 모든 몬스터에 히트박스 시각화 디버깅 코드 삽입

- CMonsterN3 y축으로의 비정상 움직임 수정

- 보스 몬스터 1종 (CMonsterB1 - 미니보스) 구현
-> 상태 별 애니메이션 적용, 높이 조정 필요

### 26.01.09 작업 내용

- 타일 및 스케일 고려한 몬스터 높이 (m_fGroundY) 수정 및 공통 변수 추상화

- AI 컴포넌트의 추적 로직에서 y 요소 무시하도록 적용

```cpp
void CAIController::Compute_Distance()
{
	if (!m_pOwnerTC || !m_pTargetTC)
	{
		m_fDistance = FLT_MAX;
		return;
	}

	_vec3 vOwnerPos, vTargetPos, vDir;
	m_pOwnerTC->Get_Info(INFO_POS, &vOwnerPos);
	vOwnerPos.y = 0.f;	// y 요소 무시
	m_pTargetTC->Get_Info(INFO_POS, &vTargetPos);
	vTargetPos.y = 0.f;	// y 요소 무시
	vDir = vTargetPos - vOwnerPos;

	m_fDistance = D3DXVec3Length(&vDir);
}

_vec3 CAIController::Compute_TargetDir()
{
	if (!m_pOwnerTC || !m_pTargetTC) return _vec3{ 0.f, 0.f, 0.f };

	_vec3 vDir, vOwnerPos, vTargetPos;
	m_pOwnerTC->Get_Info(INFO_POS, &vOwnerPos);
	vOwnerPos.y = 0.f;	// y 요소 무시
	m_pTargetTC->Get_Info(INFO_POS, &vTargetPos);
	vTargetPos.y = 0.f;	// y 요소 무시
	vDir = vTargetPos - vOwnerPos;
	D3DXVec3Normalize(&vDir, &vDir);

	return vDir;
}
```

### 26.01.10 작업 내용

- 임시 코드였던 플레이어 텍스처 반전 로직을 다른 코드들과 동일하게 변경 (버텍스 버퍼 + 텍스처 좌표 변환)

- 플레이어 리소스 재추출 (스프라이트 중심 맞지 않는 오류 수정)

### 26.01.11 작업 내용

- 보스몬스터용 AI 패턴 관리 로직 설계
-> 디버그 및 패턴 흐름 제어를 용이하기 위해 덱 자료구조로 패턴 관리
-> 공격 패턴이 여러 개일 경우, 패턴덱에서 하나씩 꺼내어 사용
-> 패턴덱은 입력된 가중치 및 중복 허용 여부에 따라 추가하여 일정 개수를 유지

```cpp

class CB2_AI : public CAIController
{
private:
	typedef struct tagB1AttackPattern
	{
		CMonsterB1::MONSTER_B1_STATE eType;
		int iWeight;		// 가중치 : 패턴이 얼마나 자주 나올지를 결정
		bool bIsActive;  	// 페이즈별 활성화 여부
	}B1_ATKPATTERN;

	// ...

	void		Generate_Pattern(CMonsterB1::MONSTER_B1_STATE eState, _bool bAllowDuplicate = false);
	void		Refill_Pattern(_bool bAllowDuplicate = false);

	// ...

	// 패턴 관련
	deque<CMonsterB1::MONSTER_B1_STATE> m_patternDeque;		// 공격 패턴을 담을 덱
	vector<B1_ATKPATTERN>				m_vecAtkPatterns;	// 공격 패턴들의 정보(상태, 빈도, 활성화 여부)
	_uint								m_iDequeMinSize;	// 덱에 담을 패턴의 최소 개수
	CMonsterB1*							m_pOwner;			// 메시지 발행은 Owner만 가능(컴포넌트는 메시지채널이 없어 불가)
	_bool								m_bOnce;			// 패턴 관련 로직이 딱 한 번만 실행되도록
};

void CB1_AI::Generate_Pattern(CMonsterB1::MONSTER_B1_STATE eLastPattern, _bool bAllowDuplicate)
{
	size_t iPatternCnt = m_vecAtkPatterns.size();

	if (iPatternCnt == 0)	// error : 등록된 공격 패턴이 없음
	{
		m_patternDeque.push_back(CMonsterB1::MONSTER_B1_STATE(0));	// 패턴이 제대로 생성되지 않으면 0번(IDLE) 상태가 지속됨
		return;
	}

	_uint iTotalWeight(0);

	if (bAllowDuplicate || iPatternCnt == 1)
	{
		for (auto& pattern : m_vecAtkPatterns)
		{
			if (pattern.bIsActive)
				iTotalWeight += pattern.iWeight;
		}
	}
	else
	{
		for (auto& pattern : m_vecAtkPatterns)
		{
			if (pattern.bIsActive && (pattern.eType != eLastPattern))
				iTotalWeight += pattern.iWeight;
		}
	}

	if (iTotalWeight == 0)	// error : 활성화된 공격 패턴이 없음
	{
		m_patternDeque.push_back(CMonsterB1::MONSTER_B1_STATE(0));
		return;
	}

	_uint iRandom = Get_Rand_Int(1, iTotalWeight);
	_uint iAccumulated(0);

	for (auto& pattern : m_vecAtkPatterns)
	{
		if (!pattern.bIsActive) continue;

		if (!bAllowDuplicate && iPatternCnt > 1 && pattern.eType == eLastPattern)
			continue;

		iAccumulated += pattern.iWeight;

		if (iRandom <= iAccumulated)
		{
			// 정상 생성
			m_patternDeque.push_back(pattern.eType);
			return;
		}
	}

	// error : 정상적으로 생성되지 않음
	m_patternDeque.push_back(CMonsterB1::MONSTER_B1_STATE(0));
}

void CB1_AI::Refill_Pattern(_bool bAllowDuplicate)
{
	while (m_patternDeque.size() < m_iDequeMinSize)	// 설정해둔 최소 크기만큼 패턴을 채워놓음
	{
		CMonsterB1::MONSTER_B1_STATE eLastState;
		
		if (m_patternDeque.empty()) eLastState = CMonsterB1::B1S_SUMMON;
		else						eLastState = m_patternDeque.back();

		Generate_Pattern(eLastState, bAllowDuplicate);
	}
}

```

### 26.01.12 작업 내용

- 투사체 클래스 제작, CMonsterB1 투사체 발사 패턴 구현

```cpp

void CMonsterB1::Launch_Projectile(const _uint& iCount)
{
	if (iCount > 1000) return;

	_float fRadian = 0.f;
	_float fGap = 2.f * D3DX_PI / iCount;
	_float fProjectileSpeed = 5.f;
	_vec3 vPos{ m_vPos.x, m_vPos.y - m_fGroundY, m_vPos.z };


	for (_uint i = 0; i < iCount; ++i)
	{
		_vec3 vSpeed{ cosf(fRadian) * fProjectileSpeed, 0.f, sinf(fRadian) * fProjectileSpeed };

		CGameObject* pProjectile = CProjectile::Create(m_pGraphicDev, vPos, vSpeed, false);

		if (pProjectile)
		{
			IMessageChannel::EVENT EProjectile;	// 이벤트를 이용해 투사체 객체를 레이어에 전달
			EProjectile.strType = L"Obj.Add";
			EProjectile.eOBJID = Engine::OID_PROJECTILE;
			EProjectile.hmapData.emplace(L"Obj", pProjectile);
			EProjectile.hmapData.emplace(L"LayerTag", L"GameLogic_Layer");
			EProjectile.hmapData.emplace(L"ObjTag", L"Projectile");
			m_pMessageChannel->Publish(EProjectile);
		}

		fRadian += fGap;
	}

	// TODO: 여기에 return 문을 삽입합니다.
}

```

### 26.01.13 작업 내용

- CMonsterB1 패턴 구현
-> 소환  : 보스 주위에 몬스터 소환
-> 돌진 준비 : 플레이어 방향을 추적하면서 점점 색 변화
-> 돌진 공격 : 직선으로 돌진
-> 히트박스 및 피격 판정은 추후 적용 예정

- CMonsterB2 (메인보스) 구현 시작

```cpp

// 돌진 공격 시 텍스처 색상 혼합으로 색 변화 구현
void CMonsterB1::Set_Material()
{
	if (m_eCurState != B1S_PREPARE) return;

	_float fMax = 1.f;
	_float fRatio = min(m_fAccTime / 2.f, 1.f);
	
	// 텍스처 색상 혼합
	m_pGraphicDev->SetTextureStageState(0, D3DTSS_COLOROP, D3DTOP_ADD);	// D3DTOP_ADD : 가산 혼합
	m_pGraphicDev->SetTextureStageState(0, D3DTSS_COLORARG1, D3DTA_TEXTURE);
	m_pGraphicDev->SetTextureStageState(0, D3DTSS_COLORARG2, D3DTA_TFACTOR);

	DWORD dwCol = DWORD(255 * fRatio * fMax);
	m_pGraphicDev->SetRenderState(D3DRS_TEXTUREFACTOR, D3DCOLOR_ARGB(255, dwCol, DWORD(dwCol * 0.1f), DWORD(dwCol * 0.1f)));

	m_bMtrl = true;
}

void CMonsterB1::Reset_Material()
{
	if (!m_bMtrl) return;

	m_pGraphicDev->SetTextureStageState(0, D3DTSS_COLOROP, D3DTOP_MODULATE);
	m_pGraphicDev->SetTextureStageState(0, D3DTSS_COLORARG2, D3DTA_DIFFUSE);
	m_pGraphicDev->SetRenderState(D3DRS_TEXTUREFACTOR, D3DCOLOR_ARGB(255, 255, 255, 255));

	m_bMtrl = false;
}

```

### 26.01.14 작업 내용

- CMonsterB2 패턴 구현
-> SMASH  : 왼쪽, 오른쪽, 가운데 순으로 머리를 땅에 찍어 공격
-> SHOOT : 중력을 받는 투사체 발사
-> SPAWN : 일반 몬스터 소환

- 패턴 반복되는 버그 수정

### 26.01.15 작업 내용

- CMonsterB2 패턴 루프 설계 및 피격/사망 상태 구현
-> SPAWN 후, IDLE -> [공격 패턴] -> DIG -> ESCAPE -> IDLE -> ... 루프 구조
-> DIG 시 지하에서 움직임(무적), 일정 시간 후 ESCAPE하여 빠져나옴
-> Boss의 경우, Hp가 0이 될 시 즉시 삭제하지 않고 사망 모션 재생 후에 이미지 고정
-> CMonsterB2 IDLE 상태에서 피격 시 HIT 상태 전환, HIT 여도 공격 패턴이 지연되지 않고 나오도록 수정

- 벽 충돌처리 개선
- Move_Pos 혹은 Lerp 이동 시 벽과 겹친 프레임과 복구된 프레임이 교차로 렌더링되지 않도록 로직 순서 변경
- 이동 관련(Key_Input, Move_Roll 등) 로직 Update 상단으로 이동 (통일성 및 서순 혼란 방지)

```cpp

void	CPlayer::OnCollision(CGameObject* pObject)
{
	// ...

	if (pObject->Get_OBJID() == OID_BORDER)
	{
		_vec3 vCurPos;
		m_pTransformCom->Get_Info(INFO_POS, &vCurPos);

		Engine::CCollider* pBorderCol = dynamic_cast<Engine::CCollider*>(
			pObject->Get_Component(ID_STATIC, L"Com_Collider"));

		if (nullptr == pBorderCol)
			return;

		const Engine::AABB& borderAABB = pBorderCol->Get_AABB();

		const _float fHalf = 1.f;

		_float fOverlapX = (borderAABB.hx + fHalf) - abs(vCurPos.x - borderAABB.x);
		_float fOverlapZ = (borderAABB.hz + fHalf) - abs(vCurPos.z - borderAABB.z);

		if (fOverlapX > 0.f && fOverlapZ > 0.f)
		{
			if (fOverlapX < fOverlapZ)
			{
				// X축 보정
				if (vCurPos.x < borderAABB.x)
					vCurPos.x = borderAABB.x - borderAABB.hx - fHalf - 0.01f;
				else
					vCurPos.x = borderAABB.x + borderAABB.hx + fHalf + 0.01f;
			}
			else if(fOverlapX > fOverlapZ)
			{
				// Z축 보정
				if (vCurPos.z < borderAABB.z)
					vCurPos.z = borderAABB.z - borderAABB.hz - fHalf - 0.01f;
				else
					vCurPos.z = borderAABB.z + borderAABB.hz + fHalf + 0.01f;
			}

			m_pTransformCom->Set_Pos(vCurPos.x, vCurPos.y, vCurPos.z);
			m_pTransformCom->Update_Component(0.f);
			m_pTransformCom->Compute_Bilboard(BBD_X);
			m_vRollPos = vCurPos;
		}

		return;
	}

	// ...
}

```

### 26.01.16 작업 내용

- 몬스터 패턴 관련 수치 조정

- NPC 텍스처 파일 입출력 코드 추가

- 메인보스 공격 패턴용 CSpike 클래스 구현
-> 정해준 횟수만큼 스스로 재귀 생성하는 로직

```cpp

void CSpike::Recur_Spike()
{
	_vec3 vPos{ m_vPos.x, 0.f, m_vPos.z };

	CGameObject* pSpike = CSpike::Create(m_pGraphicDev, m_pMessageChannel, m_vPos, m_vSpeed, --m_iRecurCount, Get_Rand_Int(0, 3));

	if (pSpike)
	{
		wstring strObjTag = L"Spike" + to_wstring(m_iRecurCount);	// Spike가 속해있는 컨테이너에 추가하면 반복자 무효화 -> 반복 횟수에 따라 다른 레이어에 집어넣어서 회피

		IMessageChannel::EVENT ESpike;
		ESpike.strType = L"Obj.Add";
		ESpike.eOBJID = Engine::OID_PROJECTILE;
		ESpike.hmapData.emplace(L"Obj", pSpike);
		ESpike.hmapData.emplace(L"LayerTag", L"GameLogic_Layer");
		ESpike.hmapData.emplace(L"ObjTag", strObjTag);
		m_pMessageChannel->Publish(ESpike);
	}
}

```

- NPC (Ratau, TarotSeller, Follower) 클래스 제작 및 애니메이션에 따른 상태 값 부여

### 26.01.17 작업 내용

- 불필요한 변수 및 함수 삭제 및 일부 중복 코드 문제 해결(추상클래스에서 공통 함수 사용)
-> Player, Monster 및 하위 클래스, Item 및 하위 클래스, AIController 및 하위 클래스

- 플레이어 상태 추가
-> 플레이어 튜토리얼용 intro 애니메이션 추가, 디버그용으로 강제 전환시켜서 확인 가능(크기 맞춤 필요)
-> 플레이어 HIT 상태 구현 및 시연용 무적 기능 추가

- 던전 세팅용 몬스터 Create 함수 오버로딩

- NPC 관련 작업
-> CBrute 클래스 제작 및 애니메이션에 따른 상태 값 부여
-> CFollower 애니메이션 스프라이트 시트로 교체

### 26.01.18 작업 내용

- 작업(Work) 가능한 맵 오브젝트(CBreakableTree, CBreakableRock) 클래스 제작
-> 작업 : 벌목, 채광, 기도, 건축 등

- 추종자의 맵 오브젝트 작업 구현을 위한 InteractMgr 구현 시작
-> 캡슐화를 유지하며 Follower에게 가장 가까운 자원의 위치 전달 및 작업량 반영 목적

- 필터 전반적인 정리 및 사용하지 않는 테스트/디버깅용 코드 제거

### 26.01.19 작업 내용

- CInteractMgr 구현, IInteractable 인터페이스 클래스 추가
-> CInteractMgr의 참조카운트 관리 로직 수정
-> 작업 가능한 클래스의 다중상속용 인터페이스 IInteractable 구현

```cpp

class IInteractable
{
public:
	virtual ~IInteractable() = default;

	virtual void	Add_WorkGauge(_float fWork)	PURE;
	virtual _float	Get_WorkGauge() const		PURE;
	virtual _bool	Is_WorkComplete() const		PURE;
	virtual _vec3*	Get_WorkPos(_vec3* pWorkPos) const	PURE;
};

CGameObject* CInteractMgr::Find_Nearest(INTERACT_TYPE eType, const _vec3& vPos)
{
	auto iter = m_mapInteractables.find(eType);

	if (iter == m_mapInteractables.end()) return nullptr;

	_float fMinDistance = FLT_MAX;

	CGameObject* pNearest = nullptr;

	for (auto& pIObj : iter->second)
	{
		_vec3 vIObjPos{};
		static_cast<CTransform*>(pIObj->Get_Component(ID_DYNAMIC, L"Com_Transform"))->Get_Info(INFO_POS, &vIObjPos);


		_vec3 vTargetDir = vIObjPos - vPos;
		vTargetDir.y = 0.f;
		_float fDistance = D3DXVec3Length(&vTargetDir);

		if (fDistance < fMinDistance)
		{
			fMinDistance = fDistance;
			pNearest = pIObj;
		}
	}
	// eType에 해당하는 작업물 중 가장 가까운 것을 반환
	return pNearest;
}

_bool CInteractMgr::Apply_Work(INTERACT_TYPE eType, const _vec3& vPos, const float& fWork)
{
	CGameObject* pTarget = Find_Nearest(eType, vPos);

	if (!pTarget) return false;

	IInteractable* pIObj = dynamic_cast<IInteractable*>(pTarget);

	_float fDist;
	_vec3 vWorkPos;
	pIObj->Get_WorkPos(&vWorkPos);
	vWorkPos -= vPos;
	vWorkPos.y = 0.f;
	fDist = D3DXVec3Length(&vWorkPos);

	if (fDist > 3.f) return false;

	if (pIObj)
	{
		pIObj->Add_WorkGauge(fWork);

		if (pIObj->Is_WorkComplete())	return false;
		else							return true;
	}
	else return false;
}

void CFollower::Execute_Work(const _float& fTimeDelta)
{
	if (m_eCurState != FOLLOWER_ACTION) return;

	if (!m_bWorking)	// 작업 중이 아님 : 현재 위치 기준으로 대상 탐색 및 위치 저장
	{
		CGameObject* pTarget = CInteractMgr::GetInstance()->Find_Nearest(CInteractMgr::INTERACT_TYPE(m_eCurWork), m_vPos);

		if (pTarget)	// 타겟 존재 시 : 타겟 위치 저장 및 AI에 정보 전달, 작업 중으로 전환
		{
			CTransform* pTargetTC = static_cast<CTransform*>(pTarget->Get_Component(ID_DYNAMIC, L"Com_Transform"));
			m_pAICom->Set_TargetTransform(pTargetTC);
			pTargetTC->Get_Info(INFO_POS, &m_vWorkPos);
			m_bWorking = true;
		}
		else			// 타겟 존재 X : IDLE 상태로 전환
		{
			m_pAICom->Set_TargetTransform(nullptr);
			m_pAICom->Anim_End(m_eCurState);
			m_eCurState = FOLLOWER_IDLE;
			m_eCurWork = FOLLOWER_WORK(Get_Rand_Int(1, 5));
			return;
		}
		
	}

	// 작업 위치 기준으로 대상 탐색
	if (!CInteractMgr::GetInstance()->Apply_Work(	CInteractMgr::INTERACT_TYPE(m_eCurWork),
													m_vWorkPos,
													m_fWorkSpeed * fTimeDelta))
	{
		m_bWorking = false;	// 작업량 반영에 실패 시 작업이 끝난 것 : 작업에서 벗어남
 		m_pAICom->Anim_End(m_eCurState);
		m_pAICom->Set_TargetTransform(nullptr);
		m_eCurState = FOLLOWER_IDLE;

		// BUILD 완료 후 이전 작업으로 복귀
		if (FW_BUILD == m_eCurWork && m_eSavedWork != FW_NONE)
		{
			m_eCurWork = m_eSavedWork;
			m_eSavedWork = FW_NONE;
		}
		//else
		//{
		//	m_eCurWork = FOLLOWER_WORK(Get_Rand_Int(1, 5));
		//}

		m_ePreWork = FW_NONE;
	}
}
```

- CEffect/CEffectMgr 클래스 추가, 이펙트 출력 디버그 키 추가
