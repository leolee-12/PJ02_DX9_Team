# 프레임워크 기록지? 보고서?

## 매일마다 작업 내용 기록용

### 25.12.14 작업 내용

- 메세지 채널 추가

- 현재 수업코드는 매니지먼트에서 접근 가능한게 컴포넌트밖에없음

- 컴포넌트 외의 객체들은 각자 멀리떨어진 객체들을 알 수가 없음 (가까워도 알 수 없지만 알려줄 수는 있음)

- 알 수 있게 할 수는 있지만, 결합도가 너무 높아짐

- 따라서 씬이 발행하는 메세지 채널을 씬에 존재하는 객체들이 참조하여 해당씬에 전역적으로 공유 할 수 있는 이벤트,메세지를 보낼 수 있는 채널 생성

### 25.12.16 작업 내용

- 메세지채널 구독기능은 구현했지만

- 구독 해제 기능은 구현하지않아서 추가

### 25.12.17 작업 내용

- 알파소팅, 빌보드, 조명매니저 병합

### 25.12.18 작업 내용

- map 키값 상수 문자열 포인터 에서 wstring으로 변경

### 25.12.19 작업 내용

- 콜리전 매니저 구현 완

- CBase -> Release 함수 내부 조건 오류 발견하여 수정

### 25.12.20 작업 내용

- 콜리전 매니저 내부 컨테이너 순회중 삭제시 삭제하려는 객체의 Free에서 콜리전 매니저에 접근시 반복자 무효화 발견

- 콜리전 매니저에서 내부 컨테이너 순회도중 외부에서 삭제요청이 들어올시 매니저에 보관해두었다가 컨테이너 순회가 끝나면 일괄삭제하는 방식으로 변경

- 씬 전환 테스트

### 25.12.21 작업 내용

- 로딩씬 추가. 로딩쓰레드는 로딩씬만 소유 할 수 있음

- 씬전환은 로딩씬을 통해서 이루어지도록.

- 로딩씬의 로딩UI, 연출화면은 UI작업이 완료되는대로 적용예정 (직교투영이 적용되어야함)

- 임시로 텍스쳐랑 기존 로고 이미지 0,0 에 사이즈 1로 띄워둠

- 퍼시스턴스매니저 추가

- 씬전환시 삭제하고 재생성하기 무거운 객체들을 관리하는 싱글톤 클래스

- 예시. 플레이어는 매 씬마다 존재해야하지만 씬전환시에 매번 생성,삭제 부담스러움

- 퍼시스턴스 매니저가 최초 1회 생성후 포인터 보관

- 씬들을 생성시에 플레이어를 레이어에 insert해줄때에 퍼시스턴스 매니저한테 플레이어 포인터를 받아와서 insert

- 소유자가 아닌 다른 객체가 참조하여 사용을 하기때문에 매니저한테 플레이어 포인터를 받아와서 레이어에 등록 할 때에 꼭 AddRef 할것!!! 카운트 감소는 씬 삭제시 알아서 함!!!

### 25.12.22 작업 내용

- 렌더러에 ui그룹 직교투영 적용

- ui그룹도 알파소팅적용

- 다만 기존에 사용하던 깊이 계산이아닌 해당 객체의 z값을 깊이로 바로 사용

- ui는 카메라가 어디에 있든 정점기준으로 월드스페이스에 존재하게 할것이기 때문

- 로딩씬 폰트, 애니매이션 추가

- 로딩시 쓰레드에서 로딩이 이루어진 값 만큼 애니매이션 재생

- 로딩 완료시 자동 씬전환. 씬전환 이제 더 건들거 없음 End








